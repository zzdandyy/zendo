# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store/
dist
dist-ssr
*.local

# CI-only e2e build cache (cargo registry / target / node_modules host binds)
.e2e-cache/

# Claude settings (local-only secrets, keep out of repo)
.claude/settings.local.json

# Test SSH keys (generated by `make ssh-keygen` / `make start-ssh-key`)
.test-ssh/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
